# Blink

## Agent skills

### Issue tracker

Local markdown under `~/.scratch/blink/<branch>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.

### Packaging & releases

Building, packaging, verifying a UI change, or cutting a release: SPM resource gotchas, codesign layout, version/tag discipline. See `docs/agents/packaging.md`.
